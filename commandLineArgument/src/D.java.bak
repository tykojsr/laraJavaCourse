class D 
{
	public static void main(String[] args) 
	{
		System.out.println("total arguments: " + args.length);
		System.out.println("-----------------------------");
		char c1 = Character.parseChar(args[0]);
	}
}
// there is no method support for converting from string to char
// we cannot able to convert from string to char
// but we can convert fron char to string
