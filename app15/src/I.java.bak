class I 
{
	public static void main(String[] args)
	{
		if(args.length < 2)
	{
		System.out.println("supply two command line arguments");
		return;
	}
	
	int i = Integer.parseint(args[0]);
	int j = Integer.parseint(args[1]);

	int min = i < j ? i : j;
	System.out.println(" min b/w " + i + " and " + j + " is " + min); 
	}

}
